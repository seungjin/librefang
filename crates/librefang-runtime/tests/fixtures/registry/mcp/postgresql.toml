id = "postgresql"
name = "PostgreSQL"
description = "Query and manage PostgreSQL databases through the MCP server"
category = "data"
icon = "lucide:database"
tags = ["database", "sql", "relational", "postgres", "queries"]

[transport]
type = "stdio"
command = "npx"
args = ["-y", "@modelcontextprotocol/server-postgres@0.6.2"]

[[required_env]]
name = "POSTGRES_CONNECTION_STRING"
label = "PostgreSQL Connection String"
help = "A full connection URI (e.g., postgresql://user:password@host:5432/dbname)"
is_secret = true
get_url = ""

[health_check]
interval_secs = 60
unhealthy_threshold = 3

setup_instructions = """
1. Obtain your PostgreSQL connection string in the format: postgresql://user:password@host:5432/dbname.
2. Ensure the database user has the necessary read/write permissions for the tables you want to access.
3. Paste the full connection string into the POSTGRES_CONNECTION_STRING field above.
"""


[i18n.zh]
name = "PostgreSQL"
description = "通过 MCP 服务器查询与管理 PostgreSQL 数据库。"

[i18n.zh-TW]
name = "PostgreSQL"
description = "透過 MCP 伺服器查詢與管理 PostgreSQL 資料庫。"

[i18n.ja]
name = "PostgreSQL"
description = "MCP サーバー経由で PostgreSQL データベースを問い合わせ・管理。"

[i18n.ko]
name = "PostgreSQL"
description = "MCP 서버를 통해 PostgreSQL 데이터베이스를 조회 및 관리."

[i18n.de]
name = "PostgreSQL"
description = "PostgreSQL-Datenbanken über den MCP-Server abfragen und verwalten."

[i18n.es]
name = "PostgreSQL"
description = "Consulta y gestiona bases de datos PostgreSQL mediante el servidor MCP."

[i18n.fr]
name = "PostgreSQL"
description = "Interroge et gère les bases PostgreSQL via le serveur MCP."
