id = "elasticsearch"
name = "Elasticsearch"
description = "Search and manage Elasticsearch indices and documents through the MCP server"
category = "data"
icon = "lucide:search"
tags = ["search", "database", "indexing", "analytics", "full-text"]

[transport]
type = "stdio"
command = "npx"
args = ["-y", "@elastic/mcp-server-elasticsearch@0.3.1"]

[[required_env]]
name = "ELASTICSEARCH_URL"
label = "Elasticsearch URL"
help = "The base URL of your Elasticsearch cluster (e.g., https://my-cluster.es.us-east-1.aws.found.io:9243)"
is_secret = false
get_url = ""

[[required_env]]
name = "ELASTICSEARCH_API_KEY"
label = "Elasticsearch API Key"
help = "An API key with read/write permissions for the target indices"
is_secret = true
get_url = ""

[health_check]
interval_secs = 60
unhealthy_threshold = 3

setup_instructions = """
1. Obtain your Elasticsearch cluster URL from your Elastic Cloud dashboard or self-hosted instance configuration.
2. Create an API key in Kibana (Stack Management > API Keys) with appropriate index permissions.
3. Paste the cluster URL and API key into the fields above.
"""


[i18n.zh]
name = "Elasticsearch"
description = "通过 MCP 服务器搜索与管理 Elasticsearch 索引和文档。"

[i18n.zh-TW]
name = "Elasticsearch"
description = "透過 MCP 伺服器搜尋與管理 Elasticsearch 索引與文件。"

[i18n.ja]
name = "Elasticsearch"
description = "MCP サーバー経由で Elasticsearch のインデックスとドキュメントを検索・管理。"

[i18n.ko]
name = "Elasticsearch"
description = "MCP 서버를 통해 Elasticsearch 인덱스와 문서를 검색 및 관리."

[i18n.de]
name = "Elasticsearch"
description = "Suchen und Verwalten von Elasticsearch-Indizes und -Dokumenten über den MCP-Server."

[i18n.es]
name = "Elasticsearch"
description = "Busca y gestiona índices y documentos de Elasticsearch mediante el servidor MCP."

[i18n.fr]
name = "Elasticsearch"
description = "Recherche et gestion des index et documents Elasticsearch via le serveur MCP."
